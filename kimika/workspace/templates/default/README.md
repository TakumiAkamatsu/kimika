# {{project_name}}

A Kimika project. Recipes live in `recipe.yaml`, inputs in `inputs/`, results
in `results/`.

## Quick start

```bash
kimika validate recipe.yaml
kimika run recipe.yaml
```

Or open the Streamlit UI and select this project from the home page:

```bash
kimika ui
```
